<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jfsb.antwort">

    <application
        android:allowBackup="true"
        android:icon="@drawable/isotipo_t"
        android:label="@string/app_name"
        android:roundIcon="@drawable/isotipo_t"
        android:supportsRtl="true"
        android:theme="@style/Theme.Antwort">
        <activity android:name=".post.CreateActivity"></activity>
        <activity android:name=".PerfilMain" />
        <activity android:name=".navigation.DemoActivity" />
        <activity
            android:name=".MenuMain"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".Registro" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>